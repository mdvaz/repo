import { Deck } from './types';

export const TOTAL_ROUNDS = 10;
export const TARGET_CARDS_PER_PLAYER = 20; 
export const AI_THINKING_TIME_MS = 1500;

// DECKS constante eliminada. Los mazos se cargarán o generarán dinámicamente.
// export const DECKS: { [key: string]: Deck } = {}; 
